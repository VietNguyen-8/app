<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BunDung"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        
        <!-- Login Activity (Launcher) -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.BunDung.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Waiter Activity -->
        <activity
            android:name=".activities.WaiterActivity"
            android:exported="false"
            android:label="@string/waiter_title" />

        <!-- Kitchen Activity -->
        <activity
            android:name=".activities.KitchenActivity"
            android:exported="false"
            android:label="@string/kitchen_title" />

        <!-- Owner Activity -->
        <activity
            android:name=".activities.OwnerActivity"
            android:exported="false"
            android:label="@string/owner_title" />

    </application>

</manifest>